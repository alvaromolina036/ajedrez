Redisenar `main.html` como panel minimo para auditoria academica.

Debe mantener:
- Verificacion visual de sesion.
- ID y nombre del usuario autenticado.
- Busqueda de rival por nombre o ID usando `GET /users/search`.
- Creacion directa de partida usando `POST /games`.
- Apertura de partida por ID.
- Listado de partidas mediante `GET /games`.
- Navegacion al tablero `game.html`.

El frontend consume exclusivamente API REST con JWT.
