Actua como desarrollador frontend para una pantalla de tablero de ajedrez academica.

Debe mantener:
- Carga de estado con `GET /games/{id}/state`.
- Movimiento con `PUT /games/{id}/move`.
- Borrado/finalizacion con `DELETE /games/{id}`.
- Render del tablero, turno y jugadores.
- Registro local de movimientos realizados en la sesion.

Debe consumir exclusivamente la API REST con JWT.
